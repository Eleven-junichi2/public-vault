---
publish: true
tags:
  - CPlusPlus
---
[[配列（C++）]]を[[動的メモリ割り当て]]することができる。ほとんど普通の配列と同様に扱える。

```cpp
std::size_t length { 5 }; // <cstddef>
int* array { new int[length]{} };
delete[] array; // 動的確保した配列のメモリ解放
```

動的確保した配列の解放には、配列の各要素のデストラクタを実行するために`delete[]`を使う。誤って`delete`を用いると[[未定義動作]]を引き起こす。