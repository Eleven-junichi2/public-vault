---
publish: true
tags:
  - CPlusPlus
---
side effect。演算子や[[関数（C++）]]が、値を計算すること以外に生じさせる観測可能な効果。

## 例

`x = 5`: 変数 x に値 5 を割り当てる。x の変更値は、演算子の実行が終了した後でも観測可能であり、これは副作用を持つ。
`std::cout << x;`: 標準出力への出力も副作用を持つ。
