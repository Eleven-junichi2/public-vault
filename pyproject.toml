[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "public-vault"
version = "1.0.0"
description = "my public vault's notes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools]
py-modules = ["main", "sync_public_notes"]
